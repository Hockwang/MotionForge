# MotionForge 使用说明（面向使用者）

> 这份文档教你**怎么用**，不讲原理。
> 不需要任何编程或机械知识，跟着步骤走就行。
>
> **进阶用法**（🚀 一键 / 模板 / 轨迹调试 / AI 协作）→ [AI-WORKFLOW.md](AI-WORKFLOW.md)
> 想了解内部原理 → [HOW-IT-WORKS.md](HOW-IT-WORKS.md)
> 遇到问题排查 → [FLOW.md](FLOW.md)

---

## 目录

1. [MotionForge 能做什么](#1-motionforge-能做什么)
2. [打开编辑器](#2-打开编辑器)
3. [加载模型](#3-加载模型)
4. [配置关节（让模型能动）](#4-配置关节让模型能动)
5. [用 AI 自然语言做动画（推荐）](#5-用-ai-自然语言做动画推荐)
6. [用关键帧手动做动画](#6-用关键帧手动做动画)
7. [播放和预览](#7-播放和预览)
8. [导出 / 导入](#8-导出--导入)
9. [常见问题](#9-常见问题)

---

## 1. MotionForge 能做什么

给一个静态的 3D 模型（例如叉车、机械臂、AGV）**加上动画能力**，然后：

- 用**自然语言**描述动作（"叉齿转 45 度，升降 0.6 米"）→ AI 自动生成动画
- 或用**关键帧**手动编排动作
- 最后导出**标准化的运动包 ZIP**，可以被其他系统消费

典型应用：工业设备的操作演示、AGV 动作标注、机器人动作脚本生成。

---

## 2. 打开编辑器

### Windows 一键启动

- 双击项目文件夹里的 `start-motionforge-with-converter.bat`
- 它会自动启动编辑器 + USD/FBX 转换服务（第一次需要安装 Blender，见下面）
- 浏览器会自动打开编辑器界面

### 如果要用 USD 或 FBX 模型

需要本地装 Blender：
- 下载安装 Blender（https://www.blender.org/download/）
- 验证过的路径：`C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`

如果只用 GLB/GLTF/USDZ 模型，不需要 Blender。

---

## 3. 加载模型

1. 界面左上"**加载资产**"区域 → 点"选择文件"
2. 选你的 3D 模型文件：
   - **直接支持**：`.glb`、`.gltf`、`.usdz`
   - **自动转换**：`.usd`、`.usda`、`.usdc`、`.fbx`（需要 Blender 服务）
3. 模型加载进中间 3D 视口，**左侧场景树**显示所有零件

### 视口操作
- **左键拖**：旋转视角
- **右键拖**：平移视角
- **滚轮**：缩放
- **点击零件**：选中（高亮）

---

## 4. 配置关节（让模型能动）

**关节** = 告诉编辑器"这个零件怎么动"。配关节是**一次性工作**，配完了后面做动画就不用再管了。

### 4.1 打开关节配置面板

1. 在**左侧场景树**找到你想让它动的零件
2. 零件右边有个**小图标**（默认"无"）→ 点它
3. 浮动配置面板出现

### 4.2 填 4 个关键字段

| 字段 | 意思 | 怎么选 |
|---|---|---|
| **类型** | 怎么动 | 铰链转动 → **旋转 (Revolute)**；滑动平移 → **平移 (Prismatic)**；不动 → **固定** |
| **关节父级** | 这个零件跟谁一起动 | 选它应该挂在哪个零件下面（比如叉齿挂在门架下） |
| **轴向** | 沿哪个方向运动 | 试错最快：选 X 看看动得对不对，不对换 Y 或 Z |
| **关节角色（AI 按语义匹配）** | 这个零件**在做什么** | 从下拉选（门架升降/叉齿旋转/车体前进…），没有合适的选"其他"手动输入 |

### 4.3 调整旋转中心（只有旋转关节需要）

选"旋转 (Revolute)" 后会出现"**关节原点 (Origin)**"区域：X/Y/Z 三个数字 + 两个按钮。

**作用**：告诉系统"绕哪个点旋转"。

**快速方法**：
1. 先点"**子对象中心**"按钮 → origin 自动填到零件中心
2. 拖 Joint Value 滑块测试旋转 → 如果旋转中心不对（比如叉齿应该绕铰链轴转，结果绕自己中心转），**手动微调 X/Y/Z**
3. 调的时候视口里会有一个**黄色小十字**显示 origin 位置，帮你定位

**快捷操作**：
- ↑ / ↓ 箭头键：±1
- Shift + ↑ / ↓：±10（浏览器默认）
- 直接输数字：任意精度

### 4.4 测试关节是否配对

拖**面板底部的 Joint Value 滑块**：
- 零件应该**按预期方向**运动
- 如果父级一起动、子级跟着动 → 关节链配对
- 如果只有这个零件动、子级不跟 → 检查子级的"关节父级"是不是选了这个零件

### 4.5 循环配置所有可动零件

每个能动的零件重复 4.1-4.4。一个典型叉车配 5 个关节：
- 车体（prismatic y，"车体前进"）
- 门架（prismatic z，"门架升降"）
- 升降组件（prismatic z，"门架升降"）
- 叉齿侧移（prismatic x，"叉齿侧移"）
- 叉齿旋转（revolute z，"叉齿旋转"）

---

## 5. 用 AI 自然语言做动画（推荐）

**前提**：关节都配好了，尤其**角色**字段要填（AI 靠这个匹配意图）。

### 5.1 打开 AI 面板

右侧面板找"**AI 动作生成**"区域。

### 5.2 可用关节 chips

输入框上方有**关节芯片列表**（如 `@_CS19110`、`@三向车.glb`）。点击任一个，自动插入到输入框——可以在描述里精确指定关节。

### 5.3 输入自然语言

例子：
- `"叉齿旋转 45 度，抬升 0.6 米"`
- `"整辆车前进 3 米，然后下降 0.3 米"`
- `"叉齿先侧移 0.5 米，再旋转 90 度"`
- `"@_CS19110 旋转 30 度"`（精确指定关节）

### 5.4 点"AI 生成动作"

AI 会返回：
- **PKF 参数**（可调的数值，像 `rotation_angle = 45`）
- **PKF 步骤**（每个关节的运动时序）

### 5.5 应用或微调

- 点"**应用**"→ 立即生效，可以播放预览
- 不满意 → 改输入重新生成，或直接在 PKF 参数/步骤面板里手动修改数值

### 5.6 如果 AI 说"找不到对应角色"

AI 提示你当前模型有哪些角色可用。说明你输入的动作意图和当前模型的关节对不上：
- 要么改动作描述（用它列出来的角色词）
- 要么去配关节的**角色字段**补上缺的

---

## 6. 用关键帧手动做动画

**适合场景**：AI 不好描述的复杂动作、需要精确控制时序。

### 6.1 创建片段

右侧"**动画片段**"区域 → 输入片段名 → 点"**创建片段**"。

### 6.2 在时间轴捕获关键帧

1. 手动拖 Joint Value 滑块把模型摆到**起始姿态**
2. 底部时间轴拖到 `0 秒`
3. 点"**在当前时间添加关键帧**"
4. 时间轴拖到后面（比如 `3 秒`），摆**中间姿态**，再点"添加关键帧"
5. 拖到最后（`5 秒`），摆**结束姿态**，再加一帧

时间轴拖动中，模型会**自动按关键帧插值**播放。

---

## 7. 播放和预览

底部有**播放按钮 + 时间轴**。

### 两种播放模式

- **关键帧驱动**（默认）：播放手动配的关键帧
- **PKF 驱动**（切换）：播放 AI 生成或手写的 PKF 公式

右侧 PKF 面板有"**用 PKF 驱动播放**"切换。

### 播放行为

- 点"播放" → 从当前时间开始
- 播到末尾 → **自动从 0 开始重播**（循环）
- 再点播放 = 暂停
- 从末尾点播放 → **自动回 0 再播**（避免先播后半段）

---

## 8. 导出 / 导入

### 导出 ZIP

右侧"**导出**"区域 → 点"**导出运动包 ZIP**"。

会下载一个 ZIP，里面包含：
- 模型（零位态的 GLB）
- 关节定义
- 关键帧 / PKF 数据
- 清单文件

### 导入 ZIP

回到左上"**加载资产**"区域 → 点"**导入产物 ZIP**" → 选之前导出的 ZIP。

模型 + 关节 + 动画全部**原样恢复**，可以继续编辑或直接播放。

---

## 9. 常见问题

### Q: 配了关节但模型不动
**检查**：
1. 时间轴**在播放**吗？
2. 关节**类型**是不是选了"无"或"固定"？
3. 切到了"**PKF 驱动**"但 PKF 步骤里没有这个关节？

### Q: 关节动了但方向反了
**解决**：重新打开关节配置，**轴向**换个（X→Y 或 X→Z 试）。

### Q: 旋转中心不对（旋转时零件甩飞）
**解决**：调 origin 的 X/Y/Z 数值。黄色小十字指示当前 origin 位置，把它拖到实际铰链位置。

### Q: AI 生成的动作作用到错误的关节
**原因**：关节的**角色字段**没填或填错了。AI 按角色语义匹配，不靠名字。

**解决**：打开该关节配置面板，角色下拉选对的（比如"叉齿侧移"不是"车体前进"）。

### Q: 播放 PKF 循环时第二遍开头停顿
**已修复**。如果还有，刷新页面。

### Q: 导出后再导入，模型停在动画末态
**已修复**。如果还有，刷新页面。

### Q: 导入后某些关节不动了
**原因**：可能是父子链条断了。

**检查**：按 F12 打开浏览器 Console，粘贴：
```js
__mf.getJointDefs().map(d => ({name: d.name, parent: d.parentId?.slice(0,8)}))
```
看每个关节的 parent 是不是对的。

### Q: 窗口变窄后右侧面板消失
**已修复**。按 `Ctrl + 0` 重置浏览器缩放。

### Q: 我想撤销操作
**Ctrl + Z**（只支持撤销，不支持重做）。

---

## 其他资源

- [FLOW.md](FLOW.md) — 故障定位决策树（更详细的排查步骤）
- [HOW-IT-WORKS.md](HOW-IT-WORKS.md) — 内部技术原理（给开发者）
- [README.md](README.md) — 项目总览 + 运行方式

不会用？有 bug？在项目里提 issue 或问 Claude。
